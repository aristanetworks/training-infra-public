steps:
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build',
    '-t',
    'us.gcr.io/${_GCLOUD_PROJECT}/atddocker_configservice:${_VERSION}',
    'configservice/.']
substitutions:
  _GCLOUD_PROJECT: atd-testdrivetraining-dev
  _VERSION: "2.0.0"
images: [
  'us.gcr.io/${_GCLOUD_PROJECT}/atddocker_configservice:${_VERSION}',
]
